import 'package:flutter/material.dart';

class AgeingReportPage extends StatelessWidget {
  const AgeingReportPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}